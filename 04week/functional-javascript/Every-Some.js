

function checkUsersValid(goodUsers) {
  return function allUsersValid (submittedUsers) {
        // SOLUTION GOES HERE
       
      };

    }

    module.exports = checkUsersValid;

    //console.log(checkUsersValid([1, 2, 3, 10]));


    //  return goodUsers.every(x => x < 9);
